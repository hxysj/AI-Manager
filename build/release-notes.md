## 2.0.14 变更说明

### 修复 WebDAV 云端备份大小校验

- 修正云端备份上传后的文件大小读取方式，改为解析 `HEAD` 响应中的 `Content-Length` 文件长度，不再把无响应体误判为云端文件大小为 0。
- Koofr 上传成功后不会再错误提示“上传不完整”；本地与云端文件大小确实不一致时仍会保留完整性校验和重传提示。
- 补充 WebDAV 文件大小响应头解析的回归测试，覆盖本次大文件上传误判场景。
