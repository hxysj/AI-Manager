## 变更说明

- 私有远程更新检查能力。
  客户端可以直接检测私有更新源中的新版本，不需要额外部署服务端，同时保持仓库和安装包分发范围私有。

- 设置页手动检查更新入口。
  设置面板顶部的保存按钮旁增加“检查更新”按钮，用户不需要退出软件或打开外部页面就可以主动触发版本检查。

- 托盘菜单检查更新入口。
  应用最小化到托盘后仍然可以快速确认是否有新版本，适合长期后台运行时使用。

- 托盘 logo 显示修复。
  托盘图标改回固定使用应用图标，避免 Windows 托盘对动态图片渲染不稳定导致图标空白。
